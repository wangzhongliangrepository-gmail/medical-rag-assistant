# 密钥 — 绝不提交
.env

# Python 缓存
__pycache__/
*.py[cod]

# git worktree 临时目录
/*-rerun/

# 数据集缓存
.cache/

# 大数据集（GB 级，绝不进 git；本地保留）
medical_data/

# Qdrant 本地落盘向量库（可由 ingest.py 重建）
qdrant_db/

# 运行日志 / 临时输出
*.log
*_out.txt
*_err.txt
.idea/
